'use strict'

import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  text:{
    color: 'rebeccapurple',
    fontSize: 20,
    textAlign: 'center',
    flex: 1,
    flexDirection: 'column'
  }
})